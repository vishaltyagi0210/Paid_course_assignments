#include<stdio.h>

int main(){

   for(int rows = 1; rows <= 3; rows++){
      for(int columns = 1; columns <= 19; columns++){
         if(columns > 3-rows && columns <= )
      }
      printf("\n");
   }
}